using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace UnitTestProject1
{
    [TestClass]
    public class UnitTest1
    {
        // Chciałem napisać testy, ale visual studio nie może dodać mi referencji.. Próbowałem na różne sposoby

        //[TestMethod]
        //[DataRow("[value, 123]", "value")]
        //[DataRow("[123, value]", "123")]
        //public void GetIdValue_Ok(string value, string result)
        //{
        //    var valueFromStrin = GetIdValueUtil.GetIdValue(value);

        //    Assert.AreEqual(valueFromStrin, result);
        //}
    }
}
